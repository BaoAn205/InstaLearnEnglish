<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/app_logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/app_logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.InstaLearnEnglish"
        android:usesCleartextTraffic="true">

        <!-- DUY NHẤT HomeActivity của feature:home làm Launcher -->
        <activity
            android:name="com.example.instalearnenglish.feature.home.HomeActivity"
            android:exported="true"
            android:theme="@style/Theme.MaterialComponents.DayNight.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Các Activity khác đã được di dời sang feature:home, 
             xóa bỏ khai báo ở đây để tránh xung đột Namespace -->

    </application>

</manifest>
